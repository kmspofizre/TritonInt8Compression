[project]
name = "effml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.2.0",
    "pytest>=9.0.3",
    "torch>=2.10.0",
    "tqdm>=4.67.3",
    "unsloth",
]

[tool.pytest.ini_options]
pythonpath = ["."]